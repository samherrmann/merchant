package exec

var (
	// TextEditorCmd is the command that launches the text editor.
	TextEditorCmd = []string{"vim"}
	// SpreadsheetEditorCmd is the command that launches the spreadsheet editor.
	SpreadsheetEditorCmd = []string{"vim"}
)
